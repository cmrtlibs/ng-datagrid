import {NgDatagridCancelCommandDirective} from './ng-datagrid-cancel-command.directive';

describe('NgDatagridCancelCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatagridCancelCommandDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
