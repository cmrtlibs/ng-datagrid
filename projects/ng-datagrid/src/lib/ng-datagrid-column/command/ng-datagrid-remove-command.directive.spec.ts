import { NgDatagridRemoveCommandDirective } from './ng-datagrid-remove-command.directive';

describe('NgDatagridRemoveCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatagridRemoveCommandDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
