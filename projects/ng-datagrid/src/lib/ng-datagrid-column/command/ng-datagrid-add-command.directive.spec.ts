import { NgDatagridAddCommandDirective } from './ng-datagrid-add-command.directive';

describe('NgDatagridAddCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatagridAddCommandDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
