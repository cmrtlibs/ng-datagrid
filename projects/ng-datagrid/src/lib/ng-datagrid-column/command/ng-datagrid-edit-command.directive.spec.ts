import { NgDatagridEditCommandDirective } from './ng-datagrid-edit-command.directive';

describe('NgDatagridEditCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatagridEditCommandDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
