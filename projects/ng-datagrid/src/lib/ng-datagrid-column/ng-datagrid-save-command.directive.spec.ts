import { NgDatagridSaveCommandDirective } from './ng-datagrid-save-command.directive';

describe('NgDatagridSaveCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatagridSaveCommandDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
